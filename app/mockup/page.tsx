export const metadata = { title: "Mockup — Directions de Design" };

export default function MockupPage() {
  return (
    <iframe
      src="/mockup/palettes.html"
      style={{ width: "100vw", height: "100vh", border: "none" }}
    />
  );
}
